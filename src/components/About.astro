---
import Layout from "../layouts/Layout.astro"
const stats = [
  { label: 'Projects Delivered', value: '150+' },
  { label: 'Success Rate', value: '99%' },
  { label: 'Years Experience', value: '8+' },
];
---

<Layout>

<section id="about" class="py-24 bg-slate-950 overflow-hidden">
  <div class="container mx-auto px-4">
    <div class="flex flex-col lg:flex-row items-center gap-16">
      
      <div class="relative w-full lg:w-1/2">
        <div class="absolute -top-4 -left-4 w-72 h-72 bg-orange-600/20 rounded-full blur-3xl"></div>
        <div class="relative rounded-2xl border border-white/10 bg-slate-900/50 p-2 backdrop-blur-sm">
          <img 
            src="https://images.unsplash.com/photo-1498050108023-c5249f4df085?auto=format&fit=crop&q=80&w=800" 
            alt="Coding Workspace" 
            class="rounded-xl shadow-2xl"
          />
          <div class="absolute -bottom-6 -right-6 hidden md:block p-6 bg-slate-800 border border-white/10 rounded-xl shadow-xl">
             <p class="text-orange-500 font-bold text-xl uppercase tracking-wider">Quality First</p>
             <p class="text-slate-400 text-sm">Engineered for perfection.</p>
          </div>
        </div>
      </div>

      <div class="w-full lg:w-1/2">
        <h2 class="text-indigo-400 font-semibold tracking-wide uppercase text-sm mb-4">
          Our Story
        </h2>
        <h3 class="text-4xl md:text-5xl font-bold text-white mb-6 leading-tight">
          We bridge the gap between <span class="text-orange-500">Vision</span> and Reality.
        </h3>
        <p class="text-slate-400 text-lg mb-8 leading-relaxed">
          At our core, we are architects of the digital world. We don't just write code; we solve complex business problems with elegant, high-performance web solutions. 
        </p>
        <p class="text-slate-400 text-lg mb-10 leading-relaxed">
          Using the latest tech stacks like Astro and Tailwind, we ensure your users enjoy a seamless experience that is fast, accessible, and visually stunning.
        </p>

        <div class="grid grid-cols-1 sm:grid-cols-3 gap-8">
          {stats.map((stat) => (
            <div class="border-l-2 border-orange-500 pl-4">
              <div class="text-3xl font-bold text-white">{stat.value}</div>
              <div class="text-slate-500 text-sm">{stat.label}</div>
            </div>
          ))}
        </div>
      </div>

    </div>
  </div>
</section>
</Layout>